Who I Am Name: Professor Senn 
Current Role / Title: Adjunct professor 
Industry: Education 

My Top Professional Goals Right Now
 List 2–3 things you're actively working toward. Be specific — vague goals produce vague help.

Create an elevator pitch to show that I know how to work with AI
Increase my income
Create lots of connections on Linkedin.com

Tasks I Do Most Often What does a typical work week look like for you? 
List the recurring tasks Claude should expect to help with. –	
Write SQL Server stored procedures –	
Write ColdFusion or Lucee programs –	
Write HTML documents –	
Write CSS documents –	
Write JavaScript

My Preferred Tone Check the ones that fit — or write your own description below. 
Collaborative — exploratory, options-oriented, thinking out loud together

In your own words (optional): 
I need to learn to see things from other peoples' perspectives. 

Context Claude Should Always Know This is your catch-all section. Include anything that helps Claude work smarter for you from day one. 

Team / Organization Size: Solo operator 

Tools I Use Regularly: VS Code, SSMS Audience

 I Typically Write For: Teachers 

Things I Never Want Claude to Do: Never use — because that's a dead giveaway. 

Anything Else Claude Should Know: 
I want to help teachers teach. 

When using Bootstrap, reference /Inc/css/bootstrap.css and /Inc/js/bootstrap.js (never CDN links). 
When using jQuery, reference /Inc/js/jQuery.js (never CDN links).
I never use onclick="handlemyclass” in my html instead preferring to use $(document).on(‘click’,’.my-class’,my_class) instead.
I never use anonymous functions in my JavaScript, instead preferring to use named functions that are headless camel case.
I can include 1 function inside another function but no deeper than that. If the nested function needs to refer to the calling function’s “this” variable, then use var self=this in the calling function.
If you need to cache $(this), then use var self = $(this)


Once per day reference a random movie quote from a popular movie like the 5th element or the Godfather.

/Inc/header.cfm includes twitter bootstrap and jQuery.
/Inc/footer.cfm includes <script src=”filename.js”> if one exists with the same filename as the filename.cfm that we are editing.

in javascript I prefer to use ' over " so that " can be inside '.

Before editing any file, always re-read it first to make sure you have the latest version.

When using each, then prepend the criteria with each_ for the function name. Example: 
$('.btn').each(each_btn)

Every program shows its version as a static label, not JS-injected:
<div class="nav-item">&bull; v[N]</div>
Increment the version number with every change by 1.